		MOV		AX,0x7000         ;15H中断的入口参数，第一个结构起始地址为0x70014,0x70000存放结构数
		MOV  	ES,AX
		MOV		EDI,0x14
        MOV		EBX,0
LOOP1:  MOV		ECX,0020D
		MOV		EDX,0X534D4150
		MOV		EAX,0XE820
		MOV		ESI,0

		INT 	15H
		INC  	ESI
		ADD		EDI,0X14
		JC		FIN
		CMP		EBX,0
		JE		FIN
		JMP  	LOOP1
	FIN:
		MOV		DWORD[ES:0],ESI